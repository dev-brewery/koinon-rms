# Keep the image build context small and cache-stable.
# Without this, a local npm ci / test run balloons the context to 100MB+
# and busts layer caching on every build.
node_modules
dist
dev-dist
playwright-report
test-results
e2e-results.json
coverage
*.local
