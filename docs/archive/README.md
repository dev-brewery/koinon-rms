# Archive — historical documents, do not trust

Everything in this directory is a point-in-time artifact (review reports,
fix summaries, superseded setup guides) kept for historical context only.

**Nothing here describes the current system.** Commands, file paths, config
names, and MCP server lists in these documents are stale. For current truth:

- `CLAUDE.md` (repo root) — entry point
- `docs/reference/` — canonical conventions, contracts, playbooks
- `docs/adr/` — architecture decisions
- `docs/claude/mcp-tools.md` — MCP tooling

If you are an AI agent doing broad context-gathering: skip this directory.
